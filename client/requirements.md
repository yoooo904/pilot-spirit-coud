## Packages
framer-motion | Complex animations and page transitions
clsx | Conditional class naming
tailwind-merge | Merging tailwind classes

## Notes
- Using Web Speech API for voice input (native browser support)
- Chat integration uses SSE at POST /api/conversations/:id/messages
- Session generation endpoint returns session with conversationId
- Dark aesthetic with 'Cinzel' and 'Cormorant Garamond' fonts
